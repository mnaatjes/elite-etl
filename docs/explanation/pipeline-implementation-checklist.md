## Implementation Plan Checkmarks

- [ ] **Step 1: Domain & Infrastructure Modeling (`models.py`)**
  - [ ] Define `MedallionDepth` Enum in Domain.
  - [ ] Add `location` to `DataSource` Pydantic schema and `metrics` to `JobRecord` schema.
  - [ ] Add `location` string column to `RegistryDataSource` SQLAlchemy model.
  - [ ] Add `metrics` JSON column to `RegistryJobRecord` SQLAlchemy model.
- [ ] **Step 2: Repository Data Access (`repository.py`)**
  - [ ] Add `update_source_location(source_id, location)` to `IRegistryRepository` and `RegistryRepository`.
  - [ ] Modify `get_jobs(limit)` to inject `ORDER BY started_at DESC LIMIT limit`.
  - [ ] Modify `update_job_status` to accept and commit `metrics: dict`.
- [ ] **Step 3: Event-Driven Service Logic (`service.py`)**
  - [ ] Update `BronzeService.sync_source()` to pass metrics and bump location to `BRONZE_SYNCED`.
  - [ ] Update `SilverService.normalize_source()` to pass metrics and bump location to `SILVER_NORMALIZED`.
  - [ ] Update `GoldService.aggregate_source()` to pass metrics and bump location to `GOLD_AGGREGATED`.
- [ ] **Step 4: API Endpoint Validation (`routers/jobs.py`)**
  - [ ] Update `GET /api/v1/jobs/` to accept `limit` query parameter.
- [ ] **Step 5: Mandatory Pytest Specifications**
  - [ ] Test model defaults.
  - [ ] Test repository ledger logic (limit and DESC sort).
  - [ ] Test event-driven location bumping and metrics capture.
